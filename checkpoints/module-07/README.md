# Module 6 Checkpoint - MyCollection with EF Core

This checkpoint adds persistent storage with Entity Framework Core and SQLite.

## Included in this checkpoint
- EF Core packages for SQLite and migrations
- `CollectionContext` registered with dependency injection
- `CollectionItem` updated with an `Id` primary key
- Full CRUD on the Collection page using the database
- Initial EF Core migration files
- `.gitignore` updated to exclude SQLite database files
